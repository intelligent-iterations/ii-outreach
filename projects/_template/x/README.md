# X Runtime

Fill this folder with:

- `.env` when needed
- `config.json`

Runtime state belongs in:

- `auth/`
- `tracking/`
- `output/actions/`
- `output/logs/`

If you preserve pre-refactor files, archive them under:

- `output/archive/legacy_intents/`
